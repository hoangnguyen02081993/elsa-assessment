export enum QueuePriority {
  Low = 4,
  Normal = 3,
  High = 2,
  Critical = 1,
}

export const QUEUE_PREFIX = '{prefix}:queues';
export const UPDATE_LEADER_BOARD_QUEUE = 'update-leader-board-queue';
